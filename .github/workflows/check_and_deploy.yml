name: Test and deploy
on: push
jobs:
  unit_tests:
    runs-on: [ubuntu-latest]
    steps:
      - uses: actions/checkout@v2

      - name: unit tests
        runs: ./gradlew test


      - name: build project
        runs: ./gradlew assemble
